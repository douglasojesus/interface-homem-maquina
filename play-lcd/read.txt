Neste diretório: está até a parte em que identificamos e escrevemos nas duas linhas do LCD. Também conseguimos desviar dependendo do botão acionado.

Posição 40°: primeira posição da segunda linha do LCD.

Salvar as palavras utilizadas e depois carregar em um registrador para usar nas telas.

Ideia: tendo loop e palavra, se criar um loop2 para exibir uma palavra2, da para trocar a palavra dependendo do botao que for acionado (estudar isso pois esta dando problema de tempo -> como trocar a palavra? acabar um loop e entrar no outro? -> qual o fluxo que segue?).

Sessão dia 28/12/23:

Para o switch: os botões são incrementadores e decrementadores. A cada variação de algum botão, o desvio vai para uma branch que verifica qual o estado atual do registrador (índice) e "seleciona" a próxima tela. Na próxima tela vai acontecer a mesma coisa. Vai ficar em loop até algum dos botões ser acionado.

Para exibição: uma possibilidade é exibir apenas o valor do resultado, e não uma constante na primeira linha e a variante na segunda linha.
Ex: Temperatura
    Valor

Talvez seja melhor apenas:
    Valor

A ideia de apresentar o nome da constante (Temperatura ou Umidade) pode ser interessante apenas no modo contínuo de exibição.

Update sessão desenvolvimento 28/11:

Nesta etapa conseguimos selecionar para exibir apenas na segunda linha (no caso atual, a tela é limpa e o cursor desce).

Além disso, conseguimos um loop com if. Quando b1 é clicado ele vai para a tela de Temperatura. 
Se b2 for acionado antes, vai para Umidade. b3 faz voltar para estado de inicio. 

Código em Python que tenta representar a lógica acima:


# b1, b2, b3 são entradas que só possuem valor True quando acionadas

def temperatura():
    while True:
        if (b3):
            return

def umidade():
    while True:
        if (b3):
            return

def inicio():
    while True:
        if (b1):
            temperatura()
        if (b2):
            umidade()

inicio()




